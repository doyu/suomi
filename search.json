[
  {
    "objectID": "anki.html",
    "href": "anki.html",
    "title": "Upload data via AnkiConnect",
    "section": "",
    "text": "source",
    "crumbs": [
      "Upload data via AnkiConnect"
    ]
  },
  {
    "objectID": "anki.html#eda",
    "href": "anki.html#eda",
    "title": "Upload data via AnkiConnect",
    "section": "EDA",
    "text": "EDA\n\nassert(call('version')==6)\n\ncall(\"deleteDecks\", {\"decks\":[\"NewDeck\"], \"cardsToo\": True})\nprint(call('deckNames'))\ncall('createDeck', {\"deck\":\"NewDeck\"})\nprint(call('deckNames'))\n\n['Default']\n['Default', 'NewDeck']\n\n\n\ncall(\"addNote\",{\n    \"note\": {\n        \"deckName\": \"NewDeck\",\n        \"modelName\": \"Basic\",\n        \"fields\": {\n            \"Front\": \"hello\",\n            \"Back\": \"こんにちは\",\n        },\n        \"options\": {\n            \"allowDuplicate\": False,\n        }\n}})\n\n1768677310825\n\n\n\nfrom pydantic import BaseModel\n\nclass NoteOptions(BaseModel):\n    allowDuplicate: bool = False\n\nclass NoteFields(BaseModel):\n    Front: str\n    Back: str\n\nclass Note(BaseModel):\n    deckName: str\n    modelName: str\n    fields: NoteFields\n    options: NoteOptions = NoteOptions()\n\nnote = Note(\n    deckName=\"NewDeck\",\n    modelName=\"Basic\",\n    fields=NoteFields(Front=\"hello2\", Back=\"こんにちは\")\n)\ncall(\"addNote\", {\"note\": note.model_dump()})\n\n1768677310874\n\n\n\nfrom IPython.display import JSON\n\ncards = call(\"findCards\", {\"query\": \"deck:NewDeck\"})\ninfo = call(\"cardsInfo\", {\"cards\": cards})\nJSON(info)\n\n&lt;IPython.core.display.JSON object&gt;\n\n\n\ncall(\"deleteDecks\", {\"decks\":[\"NewDeck\"], \"cardsToo\": True})\n\n\ndeleteDecksAll()\ndeckNames()\n\n['Default']\n\n\n\ncall(\"modelNames\")\n\n['Basic',\n 'Basic (and reversed card)',\n 'Basic (optional reversed card)',\n 'Basic (type in the answer)',\n 'Cloze',\n 'Finnish-EN-JA-Audio-Image',\n 'Image Occlusion']\n\n\n\ncall(\"modelFieldNames\", {\"modelName\":\"Basic (and reversed card)\"})\n\n['Front', 'Back']\n\n\n\ncall(\"modelFieldNames\", {\"modelName\":\"Finnish-EN-JA-Audio-Image\"})\n\n['Finnish', 'English', 'Japanese', 'Audio', 'Image']\n\n\n\ncall(\"deckNames\")\n\n['Default']",
    "crumbs": [
      "Upload data via AnkiConnect"
    ]
  },
  {
    "objectID": "anki.html#modelnote-type",
    "href": "anki.html#modelnote-type",
    "title": "Upload data via AnkiConnect",
    "section": "Model（Note type",
    "text": "Model（Note type\n\ncall(\"modelNames\")\n\n['Basic',\n 'Basic (and reversed card)',\n 'Basic (optional reversed card)',\n 'Basic (type in the answer)',\n 'Cloze',\n 'Finnish-EN-JA-Audio-Image',\n 'Image Occlusion']\n\n\nAnkiConnect v6では、モデルの削除はサポートされていません。\ncall(\"deleteModel\", {\"modelName\": \"Finnish-EN-JA-Audio-Image\"})\n方法: Ankiアプリから手動削除（推奨） 1. Ankiを開く 2. メニュー: Tools → Manage Note Types 3. Finnish-EN-JA-Audio-Imageを選択 4. Deleteボタンをクリック\n\nctx = create_finnish_model().model_dump()\n#JSON(ctx)\nctx[\"cardTemplates\"][0][\"Front\"]\n\n\"&lt;div style='font-size:34px'&gt;{{Finnish}}&lt;/div&gt;{{#Audio}}&lt;div&gt;{{Audio}}&lt;/div&gt;{{/Audio}}\"\n\n\n\naudio_field()\n\n'{{#Audio}}&lt;div&gt;{{Audio}}&lt;/div&gt;{{/Audio}}'\n\n\nctx = {\n      \"modelName\": \"Finnish-EN-JA-Audio-Image\",\n      \"inOrderFields\": [\"Finnish\",\"English\",\"Japanese\",\"Audio\",\"Image\"],\n      \"css\": \".card{font-family:arial;font-size:24px}.small{font-size:18px;color:#666} img{max-width:100%;height:auto}\",\n      \"cardTemplates\":[\n        {\n          \"Name\":\"FI → EN/JA\",\n          \"Front\":\"&lt;div style='font-size:34px'&gt;{{Finnish}}&lt;/div&gt;{{#Audio}}&lt;div&gt;{{Audio}}&lt;/div&gt;{{/Audio}}\",\n          \"Back\":\"&lt;div style='font-size:34px'&gt;{{Finnish}}&lt;/div&gt;&lt;hr&gt;&lt;div&gt;{{English}}&lt;/div&gt;&lt;div&gt;{{Japanese}}&lt;/div&gt;{{#Image}}&lt;div style='margin-top:10px'&gt;&lt;img src='{{Image}}'&gt;&lt;/div&gt;{{/Image}}{{#Audio}}&lt;div style='margin-top:10px'&gt;{{Audio}}&lt;/div&gt;{{/Audio}}\"\n        },\n      ]\n    }\n#JSON(ctx)\nprint(ctx[\"cardTemplates\"][0][\"Front\"])\ncall(\"createModel\",ctx)\n\n# まず、Modelのフィールドを再確認（安全確認）\ncall(\"modelFieldNames\", {\"modelName\":\"Finnish-EN-JA-Audio-Image\"})\n\n['Finnish', 'English', 'Japanese', 'Audio', 'Image']",
    "crumbs": [
      "Upload data via AnkiConnect"
    ]
  },
  {
    "objectID": "anki.html#check-tsv-file-content",
    "href": "anki.html#check-tsv-file-content",
    "title": "Upload data via AnkiConnect",
    "section": "Check TSV file content",
    "text": "Check TSV file content\n\ntsv = \"05_Lääkärissä.tsv\"\ntsv.split('_')\n\n['05', 'Lääkärissä.tsv']\n\n\n\ndeleteDecksAll()\n\n\nimport glob\nfrom pathlib import Path\n\nfor tsv in glob.glob('tsvs/*.tsv'):\n    p1,ps = Path(tsv).stem.split('_')\n    deck = f\"{p1}::{ps}\"\n    addnotes(deck, tsv)",
    "crumbs": [
      "Upload data via AnkiConnect"
    ]
  },
  {
    "objectID": "core.html",
    "href": "core.html",
    "title": "core",
    "section": "",
    "text": "source",
    "crumbs": [
      "core"
    ]
  },
  {
    "objectID": "core.html#tests",
    "href": "core.html#tests",
    "title": "core",
    "section": "Tests",
    "text": "Tests\n\n# Test: ffr finds files with specified extensions\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    # Create test files\n    test_dir = Path(tmpdir) / \"test\"\n    test_dir.mkdir()\n    (test_dir / \"file1.jpg\").touch()\n    (test_dir / \"file2.png\").touch()\n    (test_dir / \"file3.mp3\").touch()\n    (test_dir / \"file4.txt\").touch()\n\n    # Test finding image files\n    result = ffr([str(test_dir)], [\".jpg\", \".png\"])\n    assert len(result) == 2, f\"Expected 2 files, got {len(result)}\"\n    assert any(\"file1.jpg\" in r for r in result), \"file1.jpg not found\"\n    assert any(\"file2.png\" in r for r in result), \"file2.png not found\"\n\n    # Test finding audio files\n    result = ffr([str(test_dir)], [\".mp3\"])\n    assert len(result) == 1, f\"Expected 1 file, got {len(result)}\"\n    assert any(\"file3.mp3\" in r for r in result), \"file3.mp3 not found\"\n\nprint(\"✓ ffr extension filtering test passed\")\n\n✓ ffr extension filtering test passed\n\n\n\n# Test: ffr handles non-existent directories gracefully\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    existing_dir = Path(tmpdir) / \"exists\"\n    non_existent_dir = Path(tmpdir) / \"does_not_exist\"\n    existing_dir.mkdir()\n    (existing_dir / \"file.jpg\").touch()\n\n    # Test with mix of existing and non-existing directories\n    result = ffr([str(existing_dir), str(non_existent_dir)], [\".jpg\"])\n    assert len(result) == 1, f\"Expected 1 file (non-existent dir should be skipped), got {len(result)}\"\n    assert any(\"file.jpg\" in r for r in result), \"file.jpg not found\"\n\nprint(\"✓ ffr non-existent directory handling test passed\")\n\n✓ ffr non-existent directory handling test passed\n\n\n\n# Test: ffr handles case-insensitive extensions\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    test_dir = Path(tmpdir) / \"test\"\n    test_dir.mkdir()\n    (test_dir / \"photo.JPG\").touch()\n    (test_dir / \"image.Png\").touch()\n    (test_dir / \"sound.MP3\").touch()\n\n    # Test case-insensitive matching\n    result = ffr([str(test_dir)], [\".jpg\", \".png\", \".mp3\"])\n    assert len(result) == 3, f\"Expected 3 files (case-insensitive), got {len(result)}\"\n    assert any(\"photo.JPG\" in r for r in result), \"photo.JPG not found\"\n    assert any(\"image.Png\" in r for r in result), \"image.Png not found\"\n    assert any(\"sound.MP3\" in r for r in result), \"sound.MP3 not found\"\n\nprint(\"✓ ffr case-insensitive extension test passed\")\n\n✓ ffr case-insensitive extension test passed\n\n\n\n# Test: ffr searches recursively in subdirectories\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    test_dir = Path(tmpdir) / \"test\"\n    sub_dir = test_dir / \"subdir\" / \"nested\"\n    sub_dir.mkdir(parents=True)\n    (test_dir / \"root.jpg\").touch()\n    (sub_dir / \"deep.jpg\").touch()\n\n    # Test recursive search\n    result = ffr([str(test_dir)], [\".jpg\"])\n    assert len(result) == 2, f\"Expected 2 files (root + nested), got {len(result)}\"\n    assert any(\"root.jpg\" in r for r in result), \"root.jpg not found\"\n    assert any(\"deep.jpg\" in r for r in result), \"deep.jpg in nested dir not found\"\n\nprint(\"✓ ffr recursive search test passed\")\n\n✓ ffr recursive search test passed\n\n\n\n# Test: ffr searches multiple directories\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    dir1 = Path(tmpdir) / \"images\"\n    dir2 = Path(tmpdir) / \"audio\"\n    dir1.mkdir()\n    dir2.mkdir()\n    (dir1 / \"photo.jpg\").touch()\n    (dir2 / \"sound.mp3\").touch()\n\n    # Test searching both directories\n    result = ffr([str(dir1), str(dir2)], [\".jpg\", \".mp3\"])\n    assert len(result) == 2, f\"Expected 2 files from both directories, got {len(result)}\"\n    assert any(\"photo.jpg\" in r for r in result), \"photo.jpg not found\"\n    assert any(\"sound.mp3\" in r for r in result), \"sound.mp3 not found\"\n\nprint(\"✓ ffr multiple directory test passed\")\n\n✓ ffr multiple directory test passed\n\n\n\n# Test: ffr filters by prefix\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    test_dir = Path(tmpdir) / \"test\"\n    test_dir.mkdir()\n    (test_dir / \"05_Keho.jpg\").touch()\n    (test_dir / \"05_Kasvot.jpg\").touch()\n    (test_dir / \"06_Ruoka.jpg\").touch()\n    (test_dir / \"other.jpg\").touch()\n\n    # Test prefix filtering\n    result = ffr([str(test_dir)], [\".jpg\"], prefix=\"05_\")\n    assert len(result) == 2, f\"Expected 2 files with prefix '05_', got {len(result)}\"\n    assert any(\"05_Keho.jpg\" in r for r in result), \"05_Keho.jpg not found\"\n    assert any(\"05_Kasvot.jpg\" in r for r in result), \"05_Kasvot.jpg not found\"\n    assert not any(\"06_Ruoka.jpg\" in r for r in result), \"06_Ruoka.jpg should be filtered out\"\n    assert not any(\"other.jpg\" in r for r in result), \"other.jpg should be filtered out\"\n\nprint(\"✓ ffr prefix filtering test passed\")\n\n✓ ffr prefix filtering test passed\n\n\n\n# Test: find_files_recursive works the same as ffr\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    test_dir = Path(tmpdir) / \"test\"\n    test_dir.mkdir()\n    (test_dir / \"file1.jpg\").touch()\n    (test_dir / \"file2.png\").touch()\n    (test_dir / \"file3.mp3\").touch()\n\n    # Both functions should return identical results\n    result_ffr = ffr([str(test_dir)], [\".jpg\", \".png\"])\n    result_full = find_files_recursive([str(test_dir)], [\".jpg\", \".png\"])\n\n    assert result_ffr == result_full, f\"ffr and find_files_recursive should return identical results\"\n    assert len(result_full) == 2, f\"Expected 2 files, got {len(result_full)}\"\n\nprint(\"✓ find_files_recursive alias test passed\")\n\n✓ find_files_recursive alias test passed",
    "crumbs": [
      "core"
    ]
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "suomi",
    "section": "",
    "text": "Suomi is a Finnish language learning tool that automatically generates Anki flashcards with: - 🗣️ Finnish text-to-speech audio (using Piper TTS) - 🌍 English and Japanese translations (using OpenAI API) - 🖼️ Optional images (auto-matched from your image directory) - 🏷️ Hierarchical tags for organized decks\nPerfect for building vocabulary decks from text files with minimal effort!",
    "crumbs": [
      "suomi"
    ]
  },
  {
    "objectID": "index.html#prerequisites",
    "href": "index.html#prerequisites",
    "title": "suomi",
    "section": "Prerequisites",
    "text": "Prerequisites\nBefore installing Suomi, you’ll need:\n\nRequired External Tools\n\nAnki with AnkiConnect plugin\n\nDownload Anki from apps.ankiweb.net\nInstall AnkiConnect: Anki → Tools → Add-ons → Get Add-ons → Code: 2055492159\nMake sure Anki is running when you sync cards\n\nPiper TTS for Finnish text-to-speech\n\nDownload from github.com/rhasspy/piper\nOr install via: pip install piper-tts\nDownload Finnish model: fi_FI-harri-low.onnx\n\nffmpeg for audio conversion\n\nUbuntu/Debian: sudo apt install ffmpeg\nmacOS: brew install ffmpeg\nWindows: Download from ffmpeg.org\n\n\n\n\nRequired API Keys\n\nOpenAI API Key for translations\n\nGet your API key from platform.openai.com/api-keys\nSet environment variable:\nexport OPENAI_API_KEY=sk-your-key-here\nOr add to ~/.bashrc / ~/.zshrc for persistence",
    "crumbs": [
      "suomi"
    ]
  },
  {
    "objectID": "index.html#developer-guide",
    "href": "index.html#developer-guide",
    "title": "suomi",
    "section": "Developer Guide",
    "text": "Developer Guide\nIf you are new to using nbdev here are some useful pointers to get you started.\n\nInstall suomi in Development mode\n# make sure suomi package is installed in development mode\n$ pip install -e .\n\n# make changes under nbs/ directory\n# ...\n\n# compile to have changes apply to suomi\n$ nbdev_prepare",
    "crumbs": [
      "suomi"
    ]
  },
  {
    "objectID": "index.html#usage",
    "href": "index.html#usage",
    "title": "suomi",
    "section": "Usage",
    "text": "Usage\n\nInstallation\nInstall latest from the GitHub repository:\n$ pip install git+https://github.com/doyu/suomi.git\nor from conda\n$ conda install -c doyu suomi\nor from pypi\n$ pip install suomi\n\n\nDocumentation",
    "crumbs": [
      "suomi"
    ]
  },
  {
    "objectID": "index.html#quick-start",
    "href": "index.html#quick-start",
    "title": "suomi",
    "section": "Quick Start",
    "text": "Quick Start\n\n1. Create a text file with Finnish words\nCreate a file words.txt with one Finnish word/phrase per line:\nkissa\nkoira\nhyvää päivää\nkiitos paljon\n\n\n2. Set your OpenAI API key\nexport OPENAI_API_KEY=sk-your-key-here\n\n\n3. Run the CLI command\nsuomi create words.txt --deck \"Finnish::Daily\" --tags \"lang::fi::animals\"\nThat’s it! The tool will: 1. Translate Finnish → English/Japanese 2. Generate MP3 audio files 3. Create an Anki deck with flashcards\n\n\n4. Check Anki\nOpen Anki and you’ll see your new deck “Finnish::Daily” with cards containing: - Finnish text on the front - Audio playback - English and Japanese translations on the back - Optional images (if found)\nFill me in please! Don’t forget code examples:\n\n1+1\n\n2",
    "crumbs": [
      "suomi"
    ]
  },
  {
    "objectID": "xlate.html",
    "href": "xlate.html",
    "title": "Translation",
    "section": "",
    "text": "source\n\n\n\ndef call(\n    cli:OpenAI, msgs:list, model:str='gpt-4o-mini', max_retries:int=3\n)-&gt;str:\n\nCall OpenAI API with exponential backoff retry.\n\nsource\n\n\n\n\ndef xtexts(\n    texts:list, # List of Finnish words/phrases\n    client:openai.OpenAI | None=None, # OpenAI client (creates new if None)\n)-&gt;list: # List of dicts with keys: Finnish, English, Japanese\n\nTranslate batch of Finnish texts to English and Japanese.\nArgs: texts: List of Finnish words/phrases to translate client: OpenAI client instance (optional, creates new if not provided)\nReturns: List of dicts with keys: Finnish, English, Japanese",
    "crumbs": [
      "Translation"
    ]
  },
  {
    "objectID": "xlate.html#translation-functions",
    "href": "xlate.html#translation-functions",
    "title": "Translation",
    "section": "",
    "text": "source\n\n\n\ndef call(\n    cli:OpenAI, msgs:list, model:str='gpt-4o-mini', max_retries:int=3\n)-&gt;str:\n\nCall OpenAI API with exponential backoff retry.\n\nsource\n\n\n\n\ndef xtexts(\n    texts:list, # List of Finnish words/phrases\n    client:openai.OpenAI | None=None, # OpenAI client (creates new if None)\n)-&gt;list: # List of dicts with keys: Finnish, English, Japanese\n\nTranslate batch of Finnish texts to English and Japanese.\nArgs: texts: List of Finnish words/phrases to translate client: OpenAI client instance (optional, creates new if not provided)\nReturns: List of dicts with keys: Finnish, English, Japanese",
    "crumbs": [
      "Translation"
    ]
  },
  {
    "objectID": "xlate.html#tests",
    "href": "xlate.html#tests",
    "title": "Translation",
    "section": "Tests",
    "text": "Tests\nNote on API Integration Tests:\nFull end-to-end tests for xtexts() and cli() require: - OPENAI_API_KEY environment variable set - Network access - Cost (actual API calls)\nFor CI/CD environments, consider: - Mocking the OpenAI API calls - Using #| eval: false to skip expensive tests - Separate integration test suite\nThe unit tests above cover the logic without requiring API calls.\n\n# Test: build_prompts creates correct format\n\ntexts = [\"kissa\", \"koira\"]\nsys_prompt, user_prompt = build_prompts(texts)\n\n# Check system prompt contains key instructions\nassert \"Finnish to English and Japanese\" in sys_prompt\nassert \"Natural and contextually appropriate\" in sys_prompt\n\n# Check user prompt contains the texts\nassert \"1. kissa\" in user_prompt\nassert \"2. koira\" in user_prompt\nassert \"JSON object\" in user_prompt\nassert '\"translations\"' in user_prompt\n\nprint(\"✓ build_prompts format test passed\")\n\n✓ build_prompts format test passed\n\n\n\n# Test: parse_response raises error on count mismatch\n\njson_one_item = '{\"translations\": [{\"Finnish\": \"a\", \"English\": \"b\", \"Japanese\": \"c\"}]}'\n\n# Expect 2 items but got 1 - should raise ValueError\ntry:\n    parse_response(json_one_item, 2)\n    assert False, \"Should raise ValueError for count mismatch\"\nexcept ValueError as e:\n    assert \"Expected 2 translations, got 1\" in str(e), f\"Wrong error message: {e}\"\n\n# Expect 0 items but got 1 - should raise ValueError\ntry:\n    parse_response(json_one_item, 0)\n    assert False, \"Should raise ValueError for count mismatch\"\nexcept ValueError as e:\n    assert \"Expected 0 translations, got 1\" in str(e)\n\nprint(\"✓ parse_response error handling tests passed\")\n\n✓ parse_response error handling tests passed\n\n\n\n# Test: parse_response handles different JSON structures\n\n# Case 1: Standard format with \"translations\" key\njson1 = '{\"translations\": [{\"Finnish\": \"pää\", \"English\": \"head\", \"Japanese\": \"頭\"}]}'\nresult1 = parse_response(json1, 1)\nassert len(result1) == 1\nassert result1[0][\"Finnish\"] == \"pää\"\nassert result1[0][\"English\"] == \"head\"\nassert result1[0][\"Japanese\"] == \"頭\"\n\n# Case 2: Direct array format\njson2 = '[{\"Finnish\": \"käsi\", \"English\": \"hand\", \"Japanese\": \"手\"}]'\nresult2 = parse_response(json2, 1)\nassert len(result2) == 1\nassert result2[0][\"Finnish\"] == \"käsi\"\nassert result2[0][\"English\"] == \"hand\"\n\n# Case 3: Fallback format (dict with non-standard key)\njson3 = '{\"data\": [{\"Finnish\": \"jalka\", \"English\": \"foot\", \"Japanese\": \"足\"}]}'\nresult3 = parse_response(json3, 1)\nassert len(result3) == 1\nassert result3[0][\"Finnish\"] == \"jalka\"\nassert result3[0][\"English\"] == \"foot\"\n\nprint(\"✓ parse_response JSON format tests passed\")\n\n✓ parse_response JSON format tests passed\n\n\n\n# Test: xtexts handles whitespace-only list\nresult = xtexts([\"  \", \"\", \"  \"])\nassert result == [], f\"Expected empty list for whitespace-only input, got {result}\"\n\nprint(\"✓ xtexts whitespace-only list test passed\")\n\n✓ xtexts whitespace-only list test passed\n\n\n\n# Test: xtexts handles empty list\nresult = xtexts([])\nassert result == [], f\"Expected empty list, got {result}\"\n\nprint(\"✓ xtexts empty list test passed\")\n\n✓ xtexts empty list test passed",
    "crumbs": [
      "Translation"
    ]
  },
  {
    "objectID": "tsv.html",
    "href": "tsv.html",
    "title": "TSV Operations",
    "section": "",
    "text": "source",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#example-usage",
    "href": "tsv.html#example-usage",
    "title": "TSV Operations",
    "section": "Example Usage",
    "text": "Example Usage\n\n# Example 2: Mix of words and phrases\nfrom suomi.xlate import xtexts\nfinnish_texts = [\"Minulla on päänsärky.\", \"Satuttaako sinua?\"]\nfname = \"tsvs/07_Test.tsv\"\ntexts2tsv(finnish_texts, fname)\ntsv_load(fname)[0]  # Get just the rows\n\n[{'Finnish': 'Minulla on päänsärky.',\n  'English': 'I have a headache.',\n  'Japanese': '頭が痛いです。(あたまがいたいです。)',\n  'mp3_path': '',\n  'img_path': '',\n  'tags': 'lang::fi'},\n {'Finnish': 'Satuttaako sinua?',\n  'English': 'Does it hurt you?',\n  'Japanese': '痛いですか？(いたいですか？)',\n  'mp3_path': '',\n  'img_path': '',\n  'tags': 'lang::fi'}]\n\n\n\nsource\n\nupdate_tsv_media_paths\n\ndef update_tsv_media_paths(\n    tsv:str, # Path to TSV file\n    dirs:list=['audio', 'images'], # Directories to search for media files\n)-&gt;None:\n\nUpdate TSV file with mp3 and image paths based on file name conventions.\nSearches for media files matching the TSV filename stem and assigns them to rows. Only non-empty rows are counted as entries (empty rows are skipped).\nFile naming rules (where {stem} = TSV filename without extension, NN = zero-padded row index):\nMP3 files (row-specific only): - Pattern: {stem}_NN.mp3 (e.g., “05_Keho_00.mp3” for row 0) - Priority: {stem}_NN.mp3 → “” (common files not used for MP3) - Searches in: dirs (recursively)\nImage files (row-specific + common fallback): - Row-specific: {stem}_NN.{png,jpg} (e.g., “05_Keho_00.png” for row 0) - If exists, use this (common files are ignored) - Common: {stem}.{png,jpg} (e.g., “05_Keho.png” for all rows) - Only used if row-specific file not found - Priority: {stem}_NN.png &gt; {stem}_NN.jpg &gt; {stem}.png &gt; {stem}.jpg &gt; “” - Searches in: dirs (recursively)\nExamples: &gt;&gt;&gt; update_tsv_media_paths(“tsvs/05_Keho.tsv”) # For row 0: # - MP3: “05_Keho_00.mp3” if exists, else “” # - Image: “05_Keho_00.png” if exists, else “05_Keho_00.jpg” if exists, # else “05_Keho.png” if exists, else “05_Keho.jpg” if exists, else “”",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#hash-functions",
    "href": "tsv.html#hash-functions",
    "title": "TSV Operations",
    "section": "Hash Functions",
    "text": "Hash Functions\nFinnish テキストのハッシュ計算（重複検出用）。",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#migration-helper",
    "href": "tsv.html#migration-helper",
    "title": "TSV Operations",
    "section": "Migration Helper",
    "text": "Migration Helper\nTSV構造の進化に対応するため、古いTSVファイルに新しい列を自動追加します。\n\n# Test: ensure_columns adds missing columns\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/old.tsv\"\n\n    # Create old TSV without finnish_hash column\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        writer = csv.DictWriter(\n            f,\n            fieldnames=[\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"img_path\", \"tags\"],\n            delimiter=\"\\t\"\n        )\n        writer.writeheader()\n        writer.writerow({\n            \"Finnish\": \"kissa\",\n            \"English\": \"cat\",\n            \"Japanese\": \"猫\",\n            \"mp3_path\": \"audio/test.mp3\",\n            \"img_path\": \"\",\n            \"tags\": \"lang::fi\"\n        })\n\n    # Run migration\n    ensure_columns(tsv)\n\n    # Verify finnish_hash column added\n    rows, fields = tsv_load(tsv)\n    assert \"finnish_hash\" in fields\n    # finnish_hash is now auto-calculated (not empty)\n    assert rows[0][\"finnish_hash\"] != \"\", \"finnish_hash should be auto-calculated\"\n    assert rows[0][\"Finnish\"] == \"kissa\"  # Data preserved\n\nprint(\"✓ ensure_columns migration test passed\")\n\n✓ ensure_columns migration test passed\n\n\n\n# Test 9: ensure_columns preserves existing hash\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    existing = \"custom12\"\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        w = csv.DictWriter(f, [\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"finnish_hash\", \"img_path\", \"tags\"], delimiter=\"\\t\")\n        w.writeheader()\n        w.writerow({\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\", \"mp3_path\": \"\", \"finnish_hash\": existing, \"img_path\": \"\", \"tags\": \"lang::fi\"})\n    ensure_columns(tsv)\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"finnish_hash\"] == existing\n\n\n# Test 8: ensure_columns auto-calculates hash\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        w = csv.DictWriter(f, [\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"img_path\", \"tags\"], delimiter=\"\\t\")\n        w.writeheader()\n        w.writerow({\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\", \"mp3_path\": \"\", \"img_path\": \"\", \"tags\": \"lang::fi\"})\n    ensure_columns(tsv)\n    rows, fields = tsv_load(tsv)\n    assert \"finnish_hash\" in fields\n    assert rows[0][\"finnish_hash\"] == calculate_finnish_hash(\"kissa\")\n\n\n# Test 5: Empty string\nh = calculate_finnish_hash(\"\")\nassert isinstance(h, str) and len(h) == 8\n\n\n# Test 4: Whitespace normalization\nh = calculate_finnish_hash(\"Hei maailma\")\nassert calculate_finnish_hash(\"Hei  maailma\") == h\nassert calculate_finnish_hash(\"  Hei maailma  \") == h\n\n\n# Test 3: Case normalization\nassert calculate_finnish_hash(\"Hei\") == calculate_finnish_hash(\"hei\")\n\n\n# Test 2: Uniqueness\nassert calculate_finnish_hash(\"kissa\") != calculate_finnish_hash(\"koira\")\n\n\n# Test 1: Deterministic hashing\nh1 = calculate_finnish_hash(\"Hei maailma\")\nh2 = calculate_finnish_hash(\"Hei maailma\")\nassert h1 == h2 and len(h1) == 8",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#new-tsv-functions",
    "href": "tsv.html#new-tsv-functions",
    "title": "TSV Operations",
    "section": "New TSV Functions",
    "text": "New TSV Functions\nTSV中心アーキテクチャの基盤関数群。差分処理とべき等性を実現します。\n\nsource\n\ntsv_finnish\n\ndef tsv_finnish(\n    texts:list, output_path:str, tags:str | list[str]='lang::fi', overwrite:bool=False\n)-&gt;None:\n\nCreate TSV with Finnish column only. Other columns are empty.\nAutomatically removes duplicates (case-insensitive, whitespace-normalized) and filters out empty/whitespace-only texts. Pre-computes finnish_hash for each entry.\nArgs: texts: Finnish words/phrases to add output_path: TSV file path to create tags: Tags to add. Default “lang::fi”. Use comma-separated string or list. Examples: “lang::fi,src::daily” or [“lang::fi”, “src::daily”] overwrite: Overwrite existing TSV if True (default: False for safety)\nExamples: &gt;&gt;&gt; tsv_finnish([“kissa”, “koira”], “vocab.tsv”) # Uses default tags=“lang::fi” &gt;&gt;&gt; tsv_finnish([“kissa”, “Kissa”, “koira”], “vocab.tsv”) # Removes duplicate “Kissa” &gt;&gt;&gt; tsv_finnish([“kissa”], “vocab.tsv”, tags=“lang::fi,src::daily”) &gt;&gt;&gt; tsv_finnish([“koira”], “vocab.tsv”, tags=[“src::class”, “level::A1”])\n\n# Test: tsv_finnish creates correct structure\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv, tags=\"src::daily\")\n\n    rows, fields = tsv_load(tsv)\n    assert fields == [\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"finnish_hash\", \"img_path\", \"tags\"]\n    assert len(rows) == 2\n    assert rows[0][\"Finnish\"] == \"kissa\"\n    assert rows[0][\"English\"] == \"\"\n    # finnish_hash is now auto-generated (not empty)\n    assert rows[0][\"finnish_hash\"] != \"\", \"finnish_hash should be auto-generated\"\n    assert rows[0][\"tags\"] == \"src::daily\"\n\nprint(\"✓ tsv_finnish structure test passed\")\n\n✓ tsv_finnish structure test passed\n\n\n\nsource\n\n\ntsv_status\n\ndef tsv_status(\n    tsv:str\n)-&gt;dict:\n\nAnalyze TSV completion status (read-only, no file modification).\nReturns: { “total_rows”: 10, “finnish_filled”: 10, “english_missing”: 2, “japanese_missing”: 2, “mp3_missing”: 3, “img_missing”: 8, “fully_complete”: 5, “completion_rate”: 0.5 }\n\n# Test: tsv_status reports missing fields\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n\n    status = tsv_status(tsv)\n    assert status[\"total_rows\"] == 2\n    assert status[\"finnish_filled\"] == 2\n    assert status[\"english_missing\"] == 2\n    assert status[\"japanese_missing\"] == 2\n    assert status[\"mp3_missing\"] == 2\n    assert status[\"img_missing\"] == 2\n    assert status[\"fully_complete\"] == 0\n    assert status[\"completion_rate\"] == 0.0\n\nprint(\"✓ tsv_status basic test passed\")\n\n✓ tsv_status basic test passed\n\n\n\n# Test: tsv_status does NOT modify the file (no side effects)\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n\n    # Create a TSV WITHOUT finnish_hash column (old format)\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        writer = csv.DictWriter(\n            f,\n            fieldnames=[\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"img_path\", \"tags\"],\n            delimiter=\"\\t\"\n        )\n        writer.writeheader()\n        writer.writerow({\n            \"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\",\n            \"mp3_path\": \"\", \"img_path\": \"\", \"tags\": \"lang::fi\"\n        })\n\n    # Record file content before\n    with open(tsv, \"rb\") as f:\n        content_before = f.read()\n\n    # Call tsv_status (should be read-only)\n    status = tsv_status(tsv)\n\n    # File content must be identical after\n    with open(tsv, \"rb\") as f:\n        content_after = f.read()\n\n    assert content_before == content_after, \\\n        \"tsv_status must not modify the file\"\n\n    # Status should still be correct\n    assert status[\"total_rows\"] == 1\n    assert status[\"finnish_filled\"] == 1\n    assert status[\"english_missing\"] == 0\n    assert status[\"fully_complete\"] == 0  # mp3_path is empty\n\nprint(\"✓ tsv_status no side effects test passed\")\n\n✓ tsv_status no side effects test passed\n\n\n\n# Test: tsv_status with partially completed TSV\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\", \"talo\"], tsv)\n\n    # Add some translations\n    rows, fields = tsv_load(tsv)\n    rows[0][\"English\"] = \"cat\"\n    rows[0][\"Japanese\"] = \"猫\"\n    rows[0][\"mp3_path\"] = \"audio/test_00.mp3\"\n    rows[1][\"English\"] = \"dog\"\n    tsv_dump(tsv, rows, fields)\n\n    status = tsv_status(tsv)\n    assert status[\"total_rows\"] == 3\n    assert status[\"finnish_filled\"] == 3\n    assert status[\"english_missing\"] == 1  # Only \"talo\" missing\n    assert status[\"japanese_missing\"] == 2  # \"koira\" and \"talo\" missing\n    assert status[\"mp3_missing\"] == 2  # \"koira\" and \"talo\" missing\n    assert status[\"fully_complete\"] == 1  # Only \"kissa\" fully complete\n    assert status[\"completion_rate\"] == 1/3\n\nprint(\"✓ tsv_status partial completion test passed\")\n\n✓ tsv_status partial completion test passed\n\n\n\n# Test: tsv_status with empty TSV\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/empty.tsv\"\n    tsv_finnish([], tsv)\n\n    status = tsv_status(tsv)\n    assert status[\"total_rows\"] == 0\n    assert status[\"finnish_filled\"] == 0\n    assert status[\"english_missing\"] == 0\n    assert status[\"fully_complete\"] == 0\n    assert status[\"completion_rate\"] == 0.0\n\nprint(\"✓ tsv_status empty TSV test passed\")\n\n✓ tsv_status empty TSV test passed\n\n\n\nsource\n\n\ntsv_xlate\n\ndef tsv_xlate(\n    tsv:str, target_langs:list=['English', 'Japanese'], skip_filled:bool=True, batch_size:int=10\n)-&gt;dict:\n\nTranslate empty fields in TSV using API.\nProcesses only rows with empty English/Japanese columns. Uses batch processing to handle API rate limits.\nArgs: tsv: TSV file path target_langs: Languages to translate (currently only [“English”, “Japanese”]) skip_filled: Skip rows that already have translations batch_size: Number of rows per API batch\nReturns: { “translated”: 8, # Successfully translated rows “skipped”: 2, # Already filled rows “errors”: 1, # Failed rows “error_details”: [ # Error details list {“row”: 3, “finnish”: “text”, “error”: “API timeout”} ] }\n\n# Test: tsv_xlate fills empty fields\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n\n    # Mock xtexts - must patch both module-level and local reference\n    import suomi.tsv\n    original_module = suomi.tsv.xtexts\n    original_local = xtexts\n    mock = lambda texts: [\n        {\"Finnish\": t, \"English\": \"cat\" if t == \"kissa\" else \"dog\",\n         \"Japanese\": \"猫\" if t == \"kissa\" else \"犬\"} for t in texts\n    ]\n    suomi.tsv.xtexts = mock\n    globals()['xtexts'] = mock\n\n    try:\n        result = tsv_xlate(tsv)\n        assert result[\"translated\"] == 2\n        assert result[\"skipped\"] == 0\n        assert result[\"errors\"] == 0\n\n        # Verify translations\n        rows, _ = tsv_load(tsv)\n        assert rows[0][\"English\"] == \"cat\"\n        assert rows[0][\"Japanese\"] == \"猫\"\n        assert rows[1][\"English\"] == \"dog\"\n    finally:\n        suomi.tsv.xtexts = original_module\n        globals()['xtexts'] = original_local\n\nprint(\"✓ tsv_xlate basic test passed\")\n\n✓ tsv_xlate basic test passed\n\n\n\n# Test: tsv_xlate skips already filled rows\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n\n    # Mock xtexts\n    import suomi.tsv\n    original_module = suomi.tsv.xtexts\n    original_local = xtexts\n    call_count = [0]\n    def mock_xtexts(texts):\n        call_count[0] += 1\n        return [{\"Finnish\": t, \"English\": \"translated\", \"Japanese\": \"翻訳\"} for t in texts]\n    suomi.tsv.xtexts = mock_xtexts\n    globals()['xtexts'] = mock_xtexts\n\n    try:\n        # First run\n        result1 = tsv_xlate(tsv)\n        assert result1[\"translated\"] == 2\n        assert result1[\"skipped\"] == 0\n        assert call_count[0] == 1\n\n        # Second run (should skip)\n        result2 = tsv_xlate(tsv)\n        assert result2[\"translated\"] == 0\n        assert result2[\"skipped\"] == 2\n        assert call_count[0] == 1  # No additional API calls\n    finally:\n        suomi.tsv.xtexts = original_module\n        globals()['xtexts'] = original_local\n\nprint(\"✓ tsv_xlate idempotency test passed\")\n\n✓ tsv_xlate idempotency test passed\n\n\n\n# Test: tsv_xlate batch processing\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    # 25 rows\n    tsv_finnish([f\"word{i}\" for i in range(25)], tsv)\n\n    # Mock xtexts and count calls\n    import suomi.tsv\n    original_module = suomi.tsv.xtexts\n    original_local = xtexts\n    call_count = [0]\n    def mock_xtexts(texts):\n        call_count[0] += 1\n        return [{\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts]\n    suomi.tsv.xtexts = mock_xtexts\n    globals()['xtexts'] = mock_xtexts\n\n    try:\n        result = tsv_xlate(tsv, batch_size=10)\n\n        # 25 rows ÷ 10 = 3 batches (10, 10, 5)\n        assert call_count[0] == 3\n        assert result[\"translated\"] == 25\n        assert result[\"errors\"] == 0\n    finally:\n        suomi.tsv.xtexts = original_module\n        globals()['xtexts'] = original_local\n\nprint(\"✓ tsv_xlate batch processing test passed\")\n\n✓ tsv_xlate batch processing test passed\n\n\n\n# Test: tsv_xlate error handling\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv = f\"{tmpdir}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\", \"talo\"], tsv)\n\n    # Mock: 2nd word fails\n    import suomi.tsv\n    original_module = suomi.tsv.xtexts\n    original_local = xtexts\n    def mock_xtexts(texts):\n        result = []\n        for t in texts:\n            if t == \"koira\":\n                raise Exception(\"API error\")\n            result.append({\"Finnish\": t, \"English\": \"ok\", \"Japanese\": \"ok\"})\n        return result\n    suomi.tsv.xtexts = mock_xtexts\n    globals()['xtexts'] = mock_xtexts\n\n    try:\n        result = tsv_xlate(tsv, batch_size=1)\n\n        assert result[\"translated\"] == 2\n        assert result[\"errors\"] == 1\n        assert len(result[\"error_details\"]) == 1\n        assert result[\"error_details\"][0][\"finnish\"] == \"koira\"\n        assert \"API error\" in result[\"error_details\"][0][\"error\"]\n    finally:\n        suomi.tsv.xtexts = original_module\n        globals()['xtexts'] = original_local\n\nprint(\"✓ tsv_xlate error handling test passed\")\n\n✓ tsv_xlate error handling test passed\n\n\n\n# Test 7: tsv_finnish removes duplicates\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"kissa\", \"Kissa\", \"  kissa  \", \"koira\"], tsv)\n    rows, _ = tsv_load(tsv)\n    assert len(rows) == 2\n    texts = {row[\"Finnish\"] for row in rows}\n    assert (\"kissa\" in texts or \"Kissa\" in texts) and \"koira\" in texts\n\n\n# Test 6: tsv_finnish auto-generates hash\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"finnish_hash\"] and rows[1][\"finnish_hash\"]\n    assert rows[0][\"finnish_hash\"] != rows[1][\"finnish_hash\"]\n\n\nsource\n\n\ntsv_en_ja\n\ndef tsv_en_ja(\n    texts_dicts:list, # Translation dicts with Finnish, English, Japanese\n    tsv:str, # TSV file to update\n)-&gt;None:\n\nMerge translations into TSV using hash matching.\nUpdates English/Japanese columns for matching Finnish texts. Uses finnish_hash for matching (case-insensitive, whitespace-normalized). Preserves existing data (mp3_path, img_path, etc).\nArgs: texts_dicts: List of dicts with keys: Finnish, English, Japanese tsv: Path to TSV file to update\nExamples: &gt;&gt;&gt; tsv_finnish([“kissa”], “vocab.tsv”) &gt;&gt;&gt; tsv_en_ja([{“Finnish”: “kissa”, “English”: “cat”, “Japanese”: “猫”}], “vocab.tsv”)\n\n# Test 13: tsv_en_ja preserves existing data\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"kissa\"], tsv)\n    # Manually add mp3_path\n    rows, fields = tsv_load(tsv)\n    rows[0][\"mp3_path\"] = \"audio/kissa.mp3\"\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        w = csv.DictWriter(f, fields, delimiter=\"\\t\")\n        w.writeheader()\n        w.writerows(rows)\n    # Add translation\n    translations = [{\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\"}]\n    tsv_en_ja(translations, tsv)\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"mp3_path\"] == \"audio/kissa.mp3\"  # Preserved\n    assert rows[0][\"English\"] == \"cat\"                # Added\n\n\nsource\n\n\ntsv_mp3\n\ndef tsv_mp3(\n    tsv:str, # TSV file to update\n    output_dir:str='audio', # Output directory for MP3 files\n    model:str | None=None, # Piper model path (auto-downloaded if None)\n)-&gt;None:\n\nGenerate MP3 files and update TSV with paths (differential update).\nOnly processes rows where mp3_path is empty and Finnish text is non-empty. Supports up to 100 rows (indices 00-99) due to 2-digit zero-padding.\nExamples: &gt;&gt;&gt; tsv_finnish([“kissa”], “vocab.tsv”) &gt;&gt;&gt; tsv_mp3(“vocab.tsv”, output_dir=“audio”) # Creates: audio/vocab_00.mp3\n\n# Test 16: tsv_mp3 file naming\nimport tempfile\nfrom pathlib import Path\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/vocab.tsv\"\n    audio_dir = Path(d) / \"audio\"\n    tsv_finnish([\"a\", \"b\", \"c\"], tsv)\n\n    # Mock\n    import suomi.mp3\n    original = suomi.mp3.text2mp3\n    def mock_text2mp3(s, mp3, model):\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_text2mp3\n\n    try:\n        tsv_mp3(tsv, output_dir=str(audio_dir))\n        rows, _ = tsv_load(tsv)\n        assert rows[0][\"mp3_path\"] == f\"{audio_dir}/vocab_00.mp3\"\n        assert rows[1][\"mp3_path\"] == f\"{audio_dir}/vocab_01.mp3\"\n        assert rows[2][\"mp3_path\"] == f\"{audio_dir}/vocab_02.mp3\"\n    finally:\n        suomi.mp3.text2mp3 = original\n\n\nsource\n\n\ntsv_img\n\ndef tsv_img(\n    tsv:str, # TSV file to update\n    dirs:list=['images'], # Directories to search for images\n)-&gt;None:\n\nSearch for images and update TSV with paths.\nSearches for existing image files matching TSV filename. Updates only rows with empty img_path (differential update). Uses existing _assign_files_by_row() for file matching.\nFile priority: row-specific .png &gt; row-specific .jpg &gt; common .png &gt; common .jpg\nArgs: tsv: TSV file path dirs: Directories to search for images\nExamples: &gt;&gt;&gt; tsv_finnish([“kissa”], “vocab.tsv”) &gt;&gt;&gt; tsv_img(“vocab.tsv”, dirs=[“images”])\n\n# Test 19: tsv_img preserves existing paths\nimport tempfile\nfrom pathlib import Path\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    img_dir = Path(d) / \"images\"\n    img_dir.mkdir()\n    (img_dir / \"test_00.png\").touch()\n\n    tsv_finnish([\"kissa\"], tsv)\n    # Manually set img_path\n    rows, fields = tsv_load(tsv)\n    rows[0][\"img_path\"] = \"custom.png\"\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        w = csv.DictWriter(f, fields, delimiter=\"\\t\")\n        w.writeheader()\n        w.writerows(rows)\n\n    tsv_img(tsv, [str(img_dir)])\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"img_path\"] == \"custom.png\"  # Preserved\n\n\n# Test 18: tsv_img multiple directories\nimport tempfile\nfrom pathlib import Path\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    img1 = Path(d) / \"img1\"\n    img2 = Path(d) / \"img2\"\n    img1.mkdir()\n    img2.mkdir()\n    (img1 / \"test_00.png\").touch()\n    (img2 / \"test_01.png\").touch()\n\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n    tsv_img(tsv, [str(img1), str(img2)])\n\n    rows, _ = tsv_load(tsv)\n    assert \"test_00.png\" in rows[0][\"img_path\"]\n    assert \"test_01.png\" in rows[1][\"img_path\"]\n\n\n# Test 17: tsv_img basic assignment\nimport tempfile\nfrom pathlib import Path\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    img_dir = Path(d) / \"images\"\n    img_dir.mkdir()\n    (img_dir / \"test_00.png\").touch()\n    (img_dir / \"test_01.jpg\").touch()\n\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n    tsv_img(tsv, [str(img_dir)])\n\n    rows, _ = tsv_load(tsv)\n    assert \"test_00.png\" in rows[0][\"img_path\"]\n    assert \"test_01.jpg\" in rows[1][\"img_path\"]\n\n\n# Test 15: tsv_mp3 skips existing paths\nimport tempfile\nfrom pathlib import Path\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    audio_dir = Path(d) / \"audio\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n\n    # Manually set mp3_path for first row\n    rows, fields = tsv_load(tsv)\n    rows[0][\"mp3_path\"] = \"existing.mp3\"\n    with open(tsv, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        w = csv.DictWriter(f, fields, delimiter=\"\\t\")\n        w.writeheader()\n        w.writerows(rows)\n\n    # Mock\n    import suomi.mp3\n    original = suomi.mp3.text2mp3\n    calls = []\n    def mock_text2mp3(s, mp3, model):\n        calls.append(s)\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_text2mp3\n\n    try:\n        tsv_mp3(tsv, output_dir=str(audio_dir))\n        rows, _ = tsv_load(tsv)\n        assert rows[0][\"mp3_path\"] == \"existing.mp3\"  # Preserved\n        assert len(calls) == 1  # Only koira processed\n        assert calls[0] == \"koira\"\n    finally:\n        suomi.mp3.text2mp3 = original\n\n\n# Test 14: tsv_mp3 basic generation\nimport tempfile\nfrom pathlib import Path\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    audio_dir = Path(d) / \"audio\"\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n\n    # Mock text2mp3 by monkeypatching\n    import suomi.mp3\n    original = suomi.mp3.text2mp3\n    calls = []\n    def mock_text2mp3(s, mp3, model):\n        calls.append((s, mp3))\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_text2mp3\n\n    try:\n        tsv_mp3(tsv, output_dir=str(audio_dir))\n        rows, _ = tsv_load(tsv)\n        assert \"test_00.mp3\" in rows[0][\"mp3_path\"]\n        assert \"test_01.mp3\" in rows[1][\"mp3_path\"]\n        assert len(calls) == 2\n        assert calls[0][0] == \"kissa\"\n        assert calls[1][0] == \"koira\"\n    finally:\n        suomi.mp3.text2mp3 = original",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#media-path-management",
    "href": "tsv.html#media-path-management",
    "title": "TSV Operations",
    "section": "Media Path Management",
    "text": "Media Path Management\nMP3生成と画像パス割り当てを段階的に実行します。\n\n# Test 12: tsv_en_ja hash matching (case-insensitive)\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"Kissa\"], tsv)  # Capital K\n    # Translation with lowercase\n    translations = [{\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\"}]\n    tsv_en_ja(translations, tsv)\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"Finnish\"] == \"Kissa\"    # Original preserved\n    assert rows[0][\"English\"] == \"cat\"       # Translation added\n\n\n# Test 11: tsv_en_ja partial match\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"kissa\", \"koira\", \"talo\"], tsv)\n    # Only translate first one\n    translations = [{\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\"}]\n    tsv_en_ja(translations, tsv)\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"English\"] == \"cat\"      # Updated\n    assert rows[1][\"English\"] == \"\"         # Not updated\n    assert rows[2][\"English\"] == \"\"         # Not updated\n\n\n# Test 10: tsv_en_ja basic translation merge\nimport tempfile\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    # Create TSV with Finnish only\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n    # Add translations\n    translations = [\n        {\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\"},\n        {\"Finnish\": \"koira\", \"English\": \"dog\", \"Japanese\": \"犬\"}\n    ]\n    tsv_en_ja(translations, tsv)\n    # Verify\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"English\"] == \"cat\"\n    assert rows[0][\"Japanese\"] == \"猫\"\n    assert rows[1][\"English\"] == \"dog\"\n    assert rows[1][\"Japanese\"] == \"犬\"",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#translation-integration",
    "href": "tsv.html#translation-integration",
    "title": "TSV Operations",
    "section": "Translation Integration",
    "text": "Translation Integration\n翻訳データ（English/Japanese）をTSVに段階的に追加します。\nNote on API Integration Tests:\nFull API integration tests require: - OPENAI_API_KEY environment variable set - Network access - Cost (actual API calls)\nFor CI/CD environments, consider: - Mocking the OpenAI API calls - Using #| eval: false to skip expensive tests - Separate integration test suite\n\n# Test: texts2tsv creates correct TSV structure with tags\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    out = f\"{tmpdir}/test.tsv\"\n    # Mock xtexts to avoid API call\n    import suomi.tsv\n    original_xtexts = suomi.tsv.xtexts\n    suomi.tsv.xtexts = lambda texts: [\n        {\"Finnish\": \"kissa\", \"English\": \"cat\", \"Japanese\": \"猫\"},\n        {\"Finnish\": \"koira\", \"English\": \"dog\", \"Japanese\": \"犬\"}\n    ]\n\n    texts2tsv([\"kissa\", \"koira\"], out, tags=\"src::daily\")\n\n    # Verify file exists\n    assert Path(out).exists()\n\n    # Verify content\n    with open(out, encoding=\"utf-8\") as f:\n        reader = csv.DictReader(f, delimiter=\"\\t\")\n        rows = list(reader)\n\n        assert len(rows) == 2\n        assert rows[0][\"Finnish\"] == \"kissa\"\n        assert rows[0][\"English\"] == \"cat\"\n        assert rows[0][\"tags\"] == \"lang::fi,src::daily\"\n        assert \"mp3_path\" in rows[0]\n        assert \"img_path\" in rows[0]\n\n    # Restore\n    suomi.tsv.xtexts = original_xtexts\n\nprint(\"✓ texts2tsv TSV output test passed\")\n\n✓ texts2tsv TSV output test passed\n\n\n\n# Test: update_tsv_media_paths with custom directories\n# NOTE: This test works when run directly but fails in nbdev_test due to import issues\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv_path = f\"{tmpdir}/test.tsv\"\n    audio_dir = Path(tmpdir) / \"custom\" / \"audio\"\n    audio_dir.mkdir(parents=True, exist_ok=True)\n\n    # Create test MP3 file\n    (audio_dir / \"test_00.mp3\").touch()\n\n    # Create test TSV\n    with open(tsv_path, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        writer = csv.DictWriter(\n            f, fieldnames=[\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"img_path\", \"tags\"],\n            delimiter=\"\\t\"\n        )\n        writer.writeheader()\n        writer.writerow({\n            \"Finnish\": \"kissa\",\n            \"English\": \"cat\",\n            \"Japanese\": \"猫\",\n            \"mp3_path\": \"\",\n            \"img_path\": \"\",\n            \"tags\": \"lang/fi\"\n        })\n\n    # Update paths with custom directories\n    update_tsv_media_paths(tsv_path, dirs=[str(audio_dir), \"images\"])\n\n    # Verify paths\n    with open(tsv_path, encoding=\"utf-8\") as f:\n        rows = list(csv.DictReader(f, delimiter=\"\\t\"))\n        assert rows[0][\"mp3_path\"] == str(audio_dir / \"test_00.mp3\")\n        assert rows[0][\"img_path\"] == \"\"\n\nprint(\"✓ update_tsv_media_paths custom directories test passed\")\n\n✓ update_tsv_media_paths custom directories test passed\n\n\n\n# Test: update_tsv_media_paths with empty TSV\nwith tempfile.TemporaryDirectory() as tmpdir:\n    tsv_path = f\"{tmpdir}/empty.tsv\"\n\n    # Create empty TSV with header only\n    with open(tsv_path, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        writer = csv.DictWriter(\n            f, fieldnames=[\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"img_path\", \"tags\"],\n            delimiter=\"\\t\"\n        )\n        writer.writeheader()\n\n    # Should not crash\n    update_tsv_media_paths(tsv_path, dirs=[\"audio\", \"images\"])\n\n    # Verify still has header\n    with open(tsv_path, encoding=\"utf-8\") as f:\n        reader = csv.DictReader(f, delimiter=\"\\t\")\n        assert reader.fieldnames == [\"Finnish\", \"English\", \"Japanese\", \"mp3_path\", \"img_path\", \"tags\"]\n        assert len(list(reader)) == 0\n\nprint(\"✓ update_tsv_media_paths empty TSV test passed\")\n\n✓ update_tsv_media_paths empty TSV test passed",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "tsv.html#complete-pipeline",
    "href": "tsv.html#complete-pipeline",
    "title": "TSV Operations",
    "section": "Complete Pipeline",
    "text": "Complete Pipeline\n全タスク（翻訳→MP3→画像）を順次実行する統合関数。\n\nsource\n\ntsv_complete\n\ndef tsv_complete(\n    tsv:str, # TSV file path\n    tasks:list=['translate', 'mp3', 'img'], # Tasks to run\n    dirs:list=['audio', 'images'], # Directories for media\n    model:str | None=None, # Piper TTS model path (auto-downloaded if None)\n)-&gt;dict:\n\nRun pipeline tasks on TSV (translate → mp3 → img).\nEach task is optional and runs only if listed in tasks. Results from each task are collected and returned.\nArgs: tsv: TSV file path tasks: List of tasks to run. Options: “translate”, “mp3”, “img” dirs: Directories for media search (used by img task) model: Piper TTS model path for mp3 generation (auto-downloaded if None)\nReturns: Dict with results keyed by task name. Example: {“translate”: {“translated”: 2, …}, “mp3”: “done”, “img”: “done”}\n\n# Test 20: tsv_complete runs all tasks\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    audio_dir = Path(d) / \"audio\"\n    img_dir = Path(d) / \"images\"\n    img_dir.mkdir()\n    (img_dir / \"test_00.png\").touch()\n\n    tsv_finnish([\"kissa\", \"koira\"], tsv)\n\n    # Mock xtexts\n    import suomi.tsv\n    orig_xtexts_m = suomi.tsv.xtexts\n    orig_xtexts_l = xtexts\n    suomi.tsv.xtexts = lambda texts: [\n        {\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts\n    ]\n    globals()['xtexts'] = suomi.tsv.xtexts\n\n    # Mock text2mp3\n    import suomi.mp3\n    orig_mp3 = suomi.mp3.text2mp3\n    def mock_mp3(s, mp3, model):\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_mp3\n\n    try:\n        result = tsv_complete(tsv, dirs=[str(audio_dir), str(img_dir)])\n\n        assert \"translate\" in result\n        assert \"mp3\" in result\n        assert \"img\" in result\n        assert result[\"translate\"][\"translated\"] == 2\n\n        # Verify TSV updated\n        rows, _ = tsv_load(tsv)\n        assert rows[0][\"English\"] == \"en\"\n        assert rows[0][\"mp3_path\"] != \"\"\n        assert \"test_00.png\" in rows[0][\"img_path\"]\n    finally:\n        suomi.tsv.xtexts = orig_xtexts_m\n        globals()['xtexts'] = orig_xtexts_l\n        suomi.mp3.text2mp3 = orig_mp3\n\nprint(\"✓ tsv_complete all tasks test passed\")\n\n✓ tsv_complete all tasks test passed\n\n\n\n# Test 21: tsv_complete selective tasks (translate only)\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"kissa\"], tsv)\n\n    # Mock xtexts\n    import suomi.tsv\n    orig_xtexts_m = suomi.tsv.xtexts\n    orig_xtexts_l = xtexts\n    suomi.tsv.xtexts = lambda texts: [\n        {\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts\n    ]\n    globals()['xtexts'] = suomi.tsv.xtexts\n\n    try:\n        result = tsv_complete(tsv, tasks=[\"translate\"])\n\n        assert \"translate\" in result\n        assert \"mp3\" not in result\n        assert \"img\" not in result\n\n        # Verify only translate ran\n        rows, _ = tsv_load(tsv)\n        assert rows[0][\"English\"] == \"en\"\n        assert rows[0][\"mp3_path\"] == \"\"  # Not generated\n    finally:\n        suomi.tsv.xtexts = orig_xtexts_m\n        globals()['xtexts'] = orig_xtexts_l\n\nprint(\"✓ tsv_complete selective tasks test passed\")\n\n✓ tsv_complete selective tasks test passed\n\n\n\n# Test 22: tsv_complete with empty tasks list\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as d:\n    tsv = f\"{d}/test.tsv\"\n    tsv_finnish([\"kissa\"], tsv)\n\n    result = tsv_complete(tsv, tasks=[])\n\n    assert result == {}\n\n    # TSV unchanged\n    rows, _ = tsv_load(tsv)\n    assert rows[0][\"English\"] == \"\"\n    assert rows[0][\"mp3_path\"] == \"\"\n\nprint(\"✓ tsv_complete empty tasks test passed\")\n\n✓ tsv_complete empty tasks test passed",
    "crumbs": [
      "TSV Operations"
    ]
  },
  {
    "objectID": "mp3.html",
    "href": "mp3.html",
    "title": "How to generate mp3 files",
    "section": "",
    "text": "source",
    "crumbs": [
      "How to generate mp3 files"
    ]
  },
  {
    "objectID": "mp3.html#eda",
    "href": "mp3.html#eda",
    "title": "How to generate mp3 files",
    "section": "EDA",
    "text": "EDA\n\nimport glob\nPath(\"audio\").mkdir(exist_ok=True)\ntsv = glob.glob('tsvs/*.tsv')[0]\ntsv = Path(tsv)\ntsv, tsv.stem\n\n(PosixPath('tsvs/07_Test.tsv'), '07_Test')\n\n\n\nfrom suomi.tsv import *\ntsv_load(tsv)\n\n([{'Finnish': 'Minulla on päänsärky.',\n   'English': 'I have a headache.',\n   'Japanese': '頭が痛いです。(あたまがいたいです。)',\n   'mp3_path': '',\n   'img_path': '',\n   'tags': 'lang::fi'},\n  {'Finnish': 'Satuttaako sinua?',\n   'English': 'Does it hurt you?',\n   'Japanese': '痛いですか？(いたいですか？)',\n   'mp3_path': '',\n   'img_path': '',\n   'tags': 'lang::fi'}],\n ['Finnish', 'English', 'Japanese', 'mp3_path', 'img_path', 'tags'])\n\n\n\ndata = []\nwith open(tsv, encoding=\"utf-8\") as f:\n    for i,r in enumerate(csv.DictReader(f, delimiter=\"\\t\")):\n        r = {k:(v or \"\").strip() for k,v in r.items()}\n        r[\"fname\"] = f\"{tsv.stem}_{i:02}\"\n        data.append(r)\n\ndata[:2]\n\n[{'Finnish': 'Minulla on päänsärky.',\n  'English': 'I have a headache.',\n  'Japanese': '頭が痛いです。(あたまがいたいです。)',\n  'mp3_path': '',\n  'img_path': '',\n  'tags': 'lang::fi',\n  'fname': '07_Test_00'},\n {'Finnish': 'Satuttaako sinua?',\n  'English': 'Does it hurt you?',\n  'Japanese': '痛いですか？(いたいですか？)',\n  'mp3_path': '',\n  'img_path': '',\n  'tags': 'lang::fi',\n  'fname': '07_Test_01'}]",
    "crumbs": [
      "How to generate mp3 files"
    ]
  },
  {
    "objectID": "mp3.html#loop-all-entries-in-a-tsv-file",
    "href": "mp3.html#loop-all-entries-in-a-tsv-file",
    "title": "How to generate mp3 files",
    "section": "Loop all entries in a TSV file",
    "text": "Loop all entries in a TSV file\n\nimport shutil\nshutil.rmtree(\"audio\", ignore_errors=True)\n\n\nfrom suomi.core import ffr\nfor tsv in ffr(['tsvs'], ['.tsv']):\n    mp3s(tsv)",
    "crumbs": [
      "How to generate mp3 files"
    ]
  },
  {
    "objectID": "mp3.html#tests",
    "href": "mp3.html#tests",
    "title": "How to generate mp3 files",
    "section": "Tests",
    "text": "Tests\n\n# Test: mp3s raises error for non-existent TSV file\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    non_existent_tsv = Path(tmpdir) / \"does_not_exist.tsv\"\n\n    try:\n        mp3s(str(non_existent_tsv))\n        assert False, \"Should have raised AssertionError for non-existent TSV\"\n    except AssertionError as e:\n        assert \"TSV file not found\" in str(e), f\"Error should mention TSV not found: {e}\"\n\nprint(\"✓ mp3s non-existent TSV error test passed\")\n\nDownloading fi_FI-harri-medium.onnx...\nSaved to /home/runner/.cache/suomi/models/fi_FI-harri-medium.onnx\n✓ mp3s non-existent TSV error test passed\n\n\n\n# Test: mp3s creates output directory if it doesn't exist\nimport tempfile\nfrom pathlib import Path\nimport csv\n\nwith tempfile.TemporaryDirectory() as tmpdir:\n    # Create a test TSV file\n    tsv_path = Path(tmpdir) / \"test.tsv\"\n    with open(tsv_path, \"w\", encoding=\"utf-8\", newline=\"\") as f:\n        writer = csv.DictWriter(f, fieldnames=[\"Finnish\", \"English\", \"Japanese\"], delimiter=\"\\t\")\n        writer.writeheader()\n        # Empty TSV - just testing directory creation\n\n    output_dir = Path(tmpdir) / \"audio_output\"\n    assert not output_dir.exists(), \"Output directory should not exist yet\"\n\n    # This should create the directory (even with empty TSV)\n    mp3s(str(tsv_path), output_dir=str(output_dir))\n\n    assert output_dir.exists(), \"Output directory should be created\"\n    assert output_dir.is_dir(), \"Output path should be a directory\"\n\nprint(\"✓ mp3s directory creation test passed\")\n\n✓ mp3s directory creation test passed\n\n\n\n# Test: text2mp3 cleans up temporary WAV file even on error\nfrom unittest.mock import patch\nfrom subprocess import CalledProcessError\nimport glob\n\ncall_count = [0]\ndef mock_run(*args, **kwargs):\n    call_count[0] += 1\n    if call_count[0] &gt;= 2:\n        raise CalledProcessError(1, \"ffmpeg\")\n\n# Snapshot WAV files in system temp directory before test\nimport tempfile as _tf\nwav_before = set(glob.glob(os.path.join(_tf.gettempdir(), \"*.wav\")))\n\nwith patch('__main__.run', side_effect=mock_run):\n    try:\n        text2mp3(\"test text\", \"/tmp/_suomi_test.mp3\")\n        assert False, \"Should have raised RuntimeError\"\n    except RuntimeError:\n        pass\n\n# No new WAV files should remain after cleanup\nwav_after = set(glob.glob(os.path.join(_tf.gettempdir(), \"*.wav\")))\nleaked = wav_after - wav_before\nassert len(leaked) == 0, f\"Temp WAV files leaked: {leaked}\"\n\nprint(\"✓ text2mp3 WAV cleanup on error test passed\")\n\n✓ text2mp3 WAV cleanup on error test passed\n\n\n\n# Test: wav2mp3 raises helpful error when conversion fails\nfrom unittest.mock import patch\nfrom subprocess import CalledProcessError\n\n# Mock run in the notebook's namespace\nmock_error = CalledProcessError(1, \"ffmpeg\")\nwith patch('__main__.run', side_effect=mock_error):\n    try:\n        wav2mp3(\"input.wav\", \"output.mp3\")\n        assert False, \"Should have raised RuntimeError\"\n    except RuntimeError as e:\n        error_msg = str(e)\n        assert \"ffmpeg conversion failed\" in error_msg, f\"Error should mention conversion failure: {error_msg}\"\n        assert \"input.wav\" in error_msg, f\"Error should mention input file: {error_msg}\"\n        assert \"output.mp3\" in error_msg, f\"Error should mention output file: {error_msg}\"\n\nprint(\"✓ wav2mp3 error handling for conversion failure test passed\")\n\n✓ wav2mp3 error handling for conversion failure test passed\n\n\n\n# Test: wav2mp3 raises helpful error when ffmpeg not found\nfrom unittest.mock import patch\nimport builtins\n\n# Mock run in the notebook's namespace\nwith patch('__main__.run', side_effect=FileNotFoundError(\"ffmpeg not found\")):\n    try:\n        wav2mp3(\"test.wav\", \"test.mp3\")\n        assert False, \"Should have raised RuntimeError\"\n    except RuntimeError as e:\n        error_msg = str(e)\n        assert \"ffmpeg not found\" in error_msg, f\"Error should mention ffmpeg: {error_msg}\"\n        assert \"apt install ffmpeg\" in error_msg or \"brew install ffmpeg\" in error_msg, \\\n            f\"Error should include installation instructions: {error_msg}\"\n\nprint(\"✓ wav2mp3 error handling for missing ffmpeg test passed\")\n\n✓ wav2mp3 error handling for missing ffmpeg test passed\n\n\n\n# Test: text2wav raises helpful error when model file missing\nfrom unittest.mock import patch, MagicMock\nfrom subprocess import CalledProcessError\n\n# Mock run in the notebook's namespace\nmock_error = CalledProcessError(1, \"piper\")\nwith patch('__main__.run', side_effect=mock_error):\n    try:\n        text2wav(\"test\", \"test.wav\", model=\"nonexistent.onnx\")\n        assert False, \"Should have raised RuntimeError\"\n    except RuntimeError as e:\n        error_msg = str(e)\n        assert \"Piper TTS failed\" in error_msg, f\"Error should mention Piper TTS failure: {error_msg}\"\n        assert \"nonexistent.onnx\" in error_msg, f\"Error should mention the model file: {error_msg}\"\n\nprint(\"✓ text2wav error handling for missing model test passed\")\n\n✓ text2wav error handling for missing model test passed\n\n\n\n# Test: text2wav raises helpful error when piper not found\nfrom unittest.mock import patch, MagicMock\nfrom subprocess import run, CalledProcessError\nimport tempfile\nfrom pathlib import Path\n\n# Mock run in the notebook's namespace\nwith patch('__main__.run', side_effect=FileNotFoundError(\"piper not found\")):\n    try:\n        text2wav(\"test\", \"test.wav\")\n        assert False, \"Should have raised RuntimeError\"\n    except RuntimeError as e:\n        error_msg = str(e)\n        assert \"Piper TTS not found\" in error_msg, f\"Error message should mention Piper TTS: {error_msg}\"\n        assert \"pip install piper-tts\" in error_msg, f\"Error should include installation instructions: {error_msg}\"\n\nprint(\"✓ text2wav error handling for missing piper test passed\")\n\n✓ text2wav error handling for missing piper test passed\n\n\n\n# Test: text2wav skips empty text\ntext2wav(\"\", \"should_not_exist.wav\", model=\"dummy.onnx\")\nassert not Path(\"should_not_exist.wav\").exists()\ntext2wav(\"   \", \"should_not_exist.wav\", model=\"dummy.onnx\")\nassert not Path(\"should_not_exist.wav\").exists()\nprint(\"✓ text2wav empty text test passed\")\n\n✓ text2wav empty text test passed",
    "crumbs": [
      "How to generate mp3 files"
    ]
  },
  {
    "objectID": "cli.html",
    "href": "cli.html",
    "title": "CLI",
    "section": "",
    "text": "source\n\nsuomi_create\n\ndef suomi_create(\n    input_file:str, # Input text file (one Finnish phrase per line)\n    output_dir:str='output', # Output directory for TSV and audio\n    overwrite:bool=False, # Overwrite existing TSV\n):\n\nCreate Anki flashcards from a Finnish text file\n\nsource\n\n\nsuomi_latest\n\ndef suomi_latest(\n    input_dir:str='texts', # Directory with text files\n    output_dir:str='output', # Output directory\n    overwrite:bool=False, # Overwrite existing\n):\n\nProcess the most recently modified text file\n\nfrom suomi.cli import suomi_create, suomi_latest\nassert callable(suomi_create)\nassert callable(suomi_latest)\nprint(\"\\u2713 CLI functions are callable\")\n\n✓ CLI functions are callable",
    "crumbs": [
      "CLI"
    ]
  },
  {
    "objectID": "workflow.html",
    "href": "workflow.html",
    "title": "Workflow Integration",
    "section": "",
    "text": "source\n\n\n\ndef register_card(\n    text:str, deck:str, tags:str | list[str]='lang::fi', output_dir:str='output', overwrite:bool=False\n)-&gt;str:\n\nRegister a single Finnish card to Anki.\nConvenience wrapper around register_cards() for single items.\n\nsource\n\n\n\n\ndef register_txt(\n    txt:str, # Path to text file with Finnish phrases (one per line)\n    output_dir:str='output', # Directory for TSV and audio output\n    overwrite:bool=False, # If True, overwrite existing TSV and Anki deck\n)-&gt;str:\n\nCreate Anki cards from a text file.\nDerives deck name and tags from the filename stem. Filenames use “::” as hierarchy separator (e.g., “Ahaa!::06::Perhe.txt” -&gt; deck “Ahaa!::06::Perhe”, tags [“Ahaa!::06::Perhe”, “Perhe”]).\n\nsource\n\n\n\n\ndef register_cards(\n    texts:list, deck:str, tags:str | list[str]='lang::fi', output_dir:str='output', overwrite:bool=False\n)-&gt;str:\n\nCreate Anki cards from Finnish texts (end-to-end workflow).\nSteps: 1. Create TSV with Finnish column (tsv_finnish) 2. Fill translations + MP3 + images (tsv_complete) 3. Upload to Anki (addnotes)\nArgs: texts: Finnish words/phrases deck: Anki deck name (e.g., “06::Daily”) tags: Tags for the cards output_dir: Directory for TSV and audio files overwrite: If True, overwrite existing TSV\nReturns: Path to created TSV file\n\n# Test 1: register_cards creates TSV and runs pipeline\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as d:\n    # Mock xtexts (translation)\n    import suomi.tsv\n    orig_xtexts_m = suomi.tsv.xtexts\n    orig_xtexts_l = globals().get('xtexts')\n    mock_xt = lambda texts: [\n        {\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts\n    ]\n    suomi.tsv.xtexts = mock_xt\n    globals()['xtexts'] = mock_xt\n\n    # Mock text2mp3 (audio)\n    import suomi.mp3\n    orig_mp3 = suomi.mp3.text2mp3\n    def mock_mp3(s, mp3, model):\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_mp3\n\n    # Mock addnotes (Anki)\n    import suomi.anki\n    orig_addnotes_m = suomi.anki.addnotes\n    orig_addnotes_l = globals().get('addnotes')\n    anki_calls = []\n    def mock_addnotes(deck, tsv):\n        anki_calls.append((deck, tsv))\n    suomi.anki.addnotes = mock_addnotes\n    globals()['addnotes'] = mock_addnotes\n\n    try:\n        tsv_path = register_cards(\n            texts=[\"kissa\", \"koira\"],\n            deck=\"TestDeck\",\n            tags=\"src::test\",\n            output_dir=d,\n        )\n\n        # TSV created\n        assert Path(tsv_path).exists()\n\n        # Translations filled\n        rows, _ = tsv_load(tsv_path)\n        assert rows[0][\"English\"] == \"en\"\n        assert rows[0][\"mp3_path\"] != \"\"\n\n        # Anki called\n        assert len(anki_calls) == 1\n        assert anki_calls[0][0] == \"TestDeck\"\n    finally:\n        suomi.tsv.xtexts = orig_xtexts_m\n        globals()['xtexts'] = orig_xtexts_l\n        suomi.mp3.text2mp3 = orig_mp3\n        suomi.anki.addnotes = orig_addnotes_m\n        globals()['addnotes'] = orig_addnotes_l\n\nprint(\"✓ register_cards test passed\")\n\nTestDeck /tmp/tmp5uyb3xm7/TestDeck.tsv\n✓ register_cards test passed\n\n\n\n# Test 2: register_card delegates to register_cards\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as d:\n    # Mock all externals\n    import suomi.tsv, suomi.mp3, suomi.anki\n    orig_xtexts_m = suomi.tsv.xtexts\n    orig_xtexts_l = globals().get('xtexts')\n    orig_mp3 = suomi.mp3.text2mp3\n    orig_addnotes_m = suomi.anki.addnotes\n    orig_addnotes_l = globals().get('addnotes')\n\n    suomi.tsv.xtexts = lambda texts: [\n        {\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts\n    ]\n    globals()['xtexts'] = suomi.tsv.xtexts\n    def mock_mp3(s, mp3, model):\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_mp3\n    suomi.anki.addnotes = lambda deck, tsv: None\n    globals()['addnotes'] = suomi.anki.addnotes\n\n    try:\n        tsv_path = register_card(\"kissa\", \"TestDeck\", \"src::test\", output_dir=d)\n        rows, _ = tsv_load(tsv_path)\n        assert len(rows) == 1\n        assert rows[0][\"Finnish\"] == \"kissa\"\n        assert rows[0][\"English\"] == \"en\"\n    finally:\n        suomi.tsv.xtexts = orig_xtexts_m\n        globals()['xtexts'] = orig_xtexts_l\n        suomi.mp3.text2mp3 = orig_mp3\n        suomi.anki.addnotes = orig_addnotes_m\n        globals()['addnotes'] = orig_addnotes_l\n\nprint(\"✓ register_card test passed\")\n\nTestDeck /tmp/tmptc5lhv_9/TestDeck.tsv\n✓ register_card test passed",
    "crumbs": [
      "Workflow Integration"
    ]
  },
  {
    "objectID": "workflow.html#main-workflow-function",
    "href": "workflow.html#main-workflow-function",
    "title": "Workflow Integration",
    "section": "",
    "text": "source\n\n\n\ndef register_card(\n    text:str, deck:str, tags:str | list[str]='lang::fi', output_dir:str='output', overwrite:bool=False\n)-&gt;str:\n\nRegister a single Finnish card to Anki.\nConvenience wrapper around register_cards() for single items.\n\nsource\n\n\n\n\ndef register_txt(\n    txt:str, # Path to text file with Finnish phrases (one per line)\n    output_dir:str='output', # Directory for TSV and audio output\n    overwrite:bool=False, # If True, overwrite existing TSV and Anki deck\n)-&gt;str:\n\nCreate Anki cards from a text file.\nDerives deck name and tags from the filename stem. Filenames use “::” as hierarchy separator (e.g., “Ahaa!::06::Perhe.txt” -&gt; deck “Ahaa!::06::Perhe”, tags [“Ahaa!::06::Perhe”, “Perhe”]).\n\nsource\n\n\n\n\ndef register_cards(\n    texts:list, deck:str, tags:str | list[str]='lang::fi', output_dir:str='output', overwrite:bool=False\n)-&gt;str:\n\nCreate Anki cards from Finnish texts (end-to-end workflow).\nSteps: 1. Create TSV with Finnish column (tsv_finnish) 2. Fill translations + MP3 + images (tsv_complete) 3. Upload to Anki (addnotes)\nArgs: texts: Finnish words/phrases deck: Anki deck name (e.g., “06::Daily”) tags: Tags for the cards output_dir: Directory for TSV and audio files overwrite: If True, overwrite existing TSV\nReturns: Path to created TSV file\n\n# Test 1: register_cards creates TSV and runs pipeline\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as d:\n    # Mock xtexts (translation)\n    import suomi.tsv\n    orig_xtexts_m = suomi.tsv.xtexts\n    orig_xtexts_l = globals().get('xtexts')\n    mock_xt = lambda texts: [\n        {\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts\n    ]\n    suomi.tsv.xtexts = mock_xt\n    globals()['xtexts'] = mock_xt\n\n    # Mock text2mp3 (audio)\n    import suomi.mp3\n    orig_mp3 = suomi.mp3.text2mp3\n    def mock_mp3(s, mp3, model):\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_mp3\n\n    # Mock addnotes (Anki)\n    import suomi.anki\n    orig_addnotes_m = suomi.anki.addnotes\n    orig_addnotes_l = globals().get('addnotes')\n    anki_calls = []\n    def mock_addnotes(deck, tsv):\n        anki_calls.append((deck, tsv))\n    suomi.anki.addnotes = mock_addnotes\n    globals()['addnotes'] = mock_addnotes\n\n    try:\n        tsv_path = register_cards(\n            texts=[\"kissa\", \"koira\"],\n            deck=\"TestDeck\",\n            tags=\"src::test\",\n            output_dir=d,\n        )\n\n        # TSV created\n        assert Path(tsv_path).exists()\n\n        # Translations filled\n        rows, _ = tsv_load(tsv_path)\n        assert rows[0][\"English\"] == \"en\"\n        assert rows[0][\"mp3_path\"] != \"\"\n\n        # Anki called\n        assert len(anki_calls) == 1\n        assert anki_calls[0][0] == \"TestDeck\"\n    finally:\n        suomi.tsv.xtexts = orig_xtexts_m\n        globals()['xtexts'] = orig_xtexts_l\n        suomi.mp3.text2mp3 = orig_mp3\n        suomi.anki.addnotes = orig_addnotes_m\n        globals()['addnotes'] = orig_addnotes_l\n\nprint(\"✓ register_cards test passed\")\n\nTestDeck /tmp/tmp5uyb3xm7/TestDeck.tsv\n✓ register_cards test passed\n\n\n\n# Test 2: register_card delegates to register_cards\nimport tempfile\nfrom pathlib import Path\n\nwith tempfile.TemporaryDirectory() as d:\n    # Mock all externals\n    import suomi.tsv, suomi.mp3, suomi.anki\n    orig_xtexts_m = suomi.tsv.xtexts\n    orig_xtexts_l = globals().get('xtexts')\n    orig_mp3 = suomi.mp3.text2mp3\n    orig_addnotes_m = suomi.anki.addnotes\n    orig_addnotes_l = globals().get('addnotes')\n\n    suomi.tsv.xtexts = lambda texts: [\n        {\"Finnish\": t, \"English\": \"en\", \"Japanese\": \"ja\"} for t in texts\n    ]\n    globals()['xtexts'] = suomi.tsv.xtexts\n    def mock_mp3(s, mp3, model):\n        Path(mp3).parent.mkdir(parents=True, exist_ok=True)\n        Path(mp3).touch()\n    suomi.mp3.text2mp3 = mock_mp3\n    suomi.anki.addnotes = lambda deck, tsv: None\n    globals()['addnotes'] = suomi.anki.addnotes\n\n    try:\n        tsv_path = register_card(\"kissa\", \"TestDeck\", \"src::test\", output_dir=d)\n        rows, _ = tsv_load(tsv_path)\n        assert len(rows) == 1\n        assert rows[0][\"Finnish\"] == \"kissa\"\n        assert rows[0][\"English\"] == \"en\"\n    finally:\n        suomi.tsv.xtexts = orig_xtexts_m\n        globals()['xtexts'] = orig_xtexts_l\n        suomi.mp3.text2mp3 = orig_mp3\n        suomi.anki.addnotes = orig_addnotes_m\n        globals()['addnotes'] = orig_addnotes_l\n\nprint(\"✓ register_card test passed\")\n\nTestDeck /tmp/tmptc5lhv_9/TestDeck.tsv\n✓ register_card test passed",
    "crumbs": [
      "Workflow Integration"
    ]
  },
  {
    "objectID": "workflow.html#example-usage",
    "href": "workflow.html#example-usage",
    "title": "Workflow Integration",
    "section": "Example Usage",
    "text": "Example Usage\n\nimport glob\nfn = [Path(o) for o in glob.glob(\"texts/*.txt\")]\nprint([(i,o) for i,o in enumerate(fn)][5])\n\nregister_txt(fn[5], overwrite=True)\n\n(5, PosixPath('texts/Ahaa!::06::Perhe.txt'))\n\n\n'output/Ahaa!_06_Perhe.tsv'\n\n\n\nfor x in fn[:2]:\n    tsv_pth = register_txt(x, overwrite=True)\n    rows, _ = tsv_load(tsv_pth)\n    print(tsv_path, rows[-1])\nsync()\n\n[{'Finnish': 'Missä sä opiskelit kotimaassasi? Mä opiskelin yliopistossa.', 'English': 'Where did you study in your home country? I studied at the university.', 'Japanese': 'あなたは母国でどこで勉強しましたか？私は大学で勉強しました。', 'mp3_path': 'audio/Ahaa!_06_TE-toimistossa_00.mp3', 'finnish_hash': 'fea4a45e', 'img_path': '', 'tags': 'Ahaa!::06::TE-toimistossa,TE-toimistossa'}, {'Finnish': 'Onko sulla työkokemusta Suomessa? Ei ole. Mä opiskelen suomea kurssilla.', 'English': \"Do you have work experience in Finland? No, I don't. I am studying Finnish in a course.\", 'Japanese': 'フィンランドでの職務経験はありますか？いいえ、ありません。私はコースでフィンランド語を勉強しています。', 'mp3_path': 'audio/Ahaa!_06_TE-toimistossa_01.mp3', 'finnish_hash': '6dcd985c', 'img_path': '', 'tags': 'Ahaa!::06::TE-toimistossa,TE-toimistossa'}]\n[{'Finnish': 'Nasim istuu ja odottaa lääkäriä.', 'English': 'Nasim is sitting and waiting for the doctor.', 'Japanese': 'ナシムは座って医者を待っています。 (ナシムはすわっていしゃをまっています。)', 'mp3_path': 'audio/Ahaa!_05_Lääkarissä_00.mp3', 'finnish_hash': 'c44a0879', 'img_path': '', 'tags': 'Ahaa!::05::Lääkarissä,Lääkarissä'}, {'Finnish': 'Nasim on väsynyt, koska hän nukkuu huonosti.', 'English': 'Nasim is tired because he sleeps poorly.', 'Japanese': 'ナシムは疲れています、なぜなら彼はよく眠れないからです。 (ナシムはつかれています、なぜならかれはよくねむれないからです。)', 'mp3_path': 'audio/Ahaa!_05_Lääkarissä_01.mp3', 'finnish_hash': '8424700d', 'img_path': '', 'tags': 'Ahaa!::05::Lääkarissä,Lääkarissä'}]\n\n\n\nfn\n\n[PosixPath('texts/Ahaa!::06::TE-toimistossa.txt'),\n PosixPath('texts/Ahaa!::05::Lääkarissä.txt'),\n PosixPath('texts/Avanto.txt'),\n PosixPath('texts/Yksikkö_Monikko.txt'),\n PosixPath('texts/täytyy0.txt'),\n PosixPath('texts/Ahaa!::06::Perhe.txt'),\n PosixPath('texts/Cafe.txt'),\n PosixPath('texts/Vartalo::Jooga.txt'),\n PosixPath('texts/Järjestysluvut.txt'),\n PosixPath('texts/täytyy.txt'),\n PosixPath('texts/Kuukaudet_Vuodenajat.txt'),\n PosixPath('texts/Ahaa!::05::Oireet.txt'),\n PosixPath('texts/Ahaa!::05::Kasvot.txt'),\n PosixPath('texts/Ahaa!::20260203.txt')]\n\n\n\nsource\n\nregister_last\n\ndef register_last(\n    n:int, input_dir:str='texts', output_dir:str='output', overwrite:bool=False\n)-&gt;list:\n\nRegister the last n text files (by modification time).\nArgs: n: Number of files to process (must be positive)\n\nsource\n\n\nregister_latest\n\ndef register_latest(\n    input_dir:str='texts', output_dir:str='output', overwrite:bool=False\n)-&gt;str:\n\nRegister the most recently modified text file.\n\n#addnotes(\"Ahaa!::06::Haluta\", \"output/Ahaa!_06_Haluta.tsv\")\n\n\nregister_latest(overwrite=False)",
    "crumbs": [
      "Workflow Integration"
    ]
  }
]